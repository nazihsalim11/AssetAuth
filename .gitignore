# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files — never commit these. They hold database passwords and
# service-role keys. Use .env.example as the template.
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# Scratch / throwaway
brain/
src_app_diff.txt
test_tickets.cjs

# Locally-uploaded files. In production these live in Supabase Storage.
# (Existing committed PDFs stay tracked; this only stops new ones.)
backend/public/uploads/
